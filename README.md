# blink
Basic Lua clocking script for monome grids running iii.

This script provides a simple clock for monome grid iii, supporting both a fixed internal clock and synchronisation to an external MIDI source. It includes an basic led indicator for clock activity and a master override to pause all clocking.

Designed as a foundation for more complex scripts that require flexible clock handling.
